---
const faqs = [
  {
    question: "What is NANA Banana Pro?",
    answer: "NANA Banana Pro is a free AI-powered image generation tool that lets you create stunning images from text descriptions or reference images. It uses advanced AI models to produce professional-quality visuals in seconds."
  },
  {
    question: "Do I need to create an account?",
    answer: "No, you can start generating images right away without signing up or creating an account. Just visit the Generate page, type your prompt, and click Generate."
  },
  {
    question: "What styles are available?",
    answer: "We offer multiple artistic styles including Photorealistic, Anime, Watercolor, Oil Painting, Digital Art, and Pixel Art. You can also let the AI decide the best style by selecting 'Auto'."
  },
  {
    question: "Can I use generated images commercially?",
    answer: "Yes, images generated with NANA Banana Pro are yours to use. You can use them for personal projects, social media, presentations, and commercial purposes."
  },
  {
    question: "What is Image to Image generation?",
    answer: "Image to Image lets you upload a reference image and use it as a starting point for AI generation. Combined with a text prompt, the AI will transform your reference into a new image guided by your description."
  },
  {
    question: "How long does it take to generate an image?",
    answer: "Most images are generated in 5-15 seconds depending on complexity and current server load. Our optimized pipeline ensures you get results as quickly as possible."
  }
];
---

<section id="faq" class="py-20 sm:py-28">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="text-center mb-16">
      <h2 class="font-display text-3xl sm:text-4xl font-bold">
        Frequently Asked <span class="text-banana-400">Questions</span>
      </h2>
      <p class="mt-4 text-gray-400 text-lg">
        Got questions? We have answers.
      </p>
    </div>

    <div class="space-y-3">
      {faqs.map((faq, index) => (
        <div class="faq-item border border-white/5 rounded-xl overflow-hidden bg-white/[0.02] hover:bg-white/[0.04] transition-colors">
          <button
            class="faq-toggle w-full flex items-center justify-between p-5 text-left"
            data-index={index}
          >
            <span class="font-medium text-white pr-4">{faq.question}</span>
            <svg class="faq-chevron w-5 h-5 text-gray-400 shrink-0 transition-transform duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7" />
            </svg>
          </button>
          <div class="faq-content hidden px-5 pb-5">
            <p class="text-gray-400 text-sm leading-relaxed">{faq.answer}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<script>
  document.querySelectorAll('.faq-toggle').forEach(btn => {
    btn.addEventListener('click', () => {
      const item = btn.closest('.faq-item');
      const content = item?.querySelector('.faq-content');
      const chevron = item?.querySelector('.faq-chevron');
      const isOpen = !content?.classList.contains('hidden');

      // Close all
      document.querySelectorAll('.faq-content').forEach(c => c.classList.add('hidden'));
      document.querySelectorAll('.faq-chevron').forEach(c => c.classList.remove('rotate-180'));

      // Toggle current
      if (!isOpen) {
        content?.classList.remove('hidden');
        chevron?.classList.add('rotate-180');
      }
    });
  });
</script>
